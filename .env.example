# This standalone terminal does not need a backend.
# Use mock mode for local demo UI and one-click local demo trades.
EXPO_PUBLIC_TERMINAL_MODE=mock

# Optional defaults for the first screen.
EXPO_PUBLIC_DEFAULT_SYMBOL=EURUSD
EXPO_PUBLIC_DEFAULT_LOTS=0.50
